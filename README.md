# Income Classification using Decision Tree

This project classifies whether a person's income is above or below a certain threshold using a Decision Tree classifier.

### 📌 Features
- Data preprocessing
- Decision Tree model training
- Model evaluation
- Predictions on new data

### 📂 Files
- `main.py` — Main script to train and test the model
- `requirements.txt` — Python dependencies

### 🚀 How to Run
```bash
pip install -r requirements.txt
python main.py
```

### 📊 Dataset
Use any income dataset (for example: `adult.csv` from UCI dataset).  
Save the dataset in the project folder as **data.csv**.

---
If you need help preparing your dataset, tell me! 😊
